!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    %meta{:content => content_for?(:description) ? yield(:description) : "Support Tickets App", :name => "description"}
    
    %title= page_title
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
    
  %body{class: body_class}
    %header.navbar
      %p.title Support Issue Tracking
      .navbar-login
        - if logged_in?
          =link_to('Log out', logout_path, method: :delete, class: 'logout')
        - else
          = render 'layouts/login_form'
      %nav
        = render 'layouts/navigation' if logged_in?
        
      = render 'layouts/searchbox' if logged_in?
        
    #main
      .messages
        = render 'layouts/messages'
      .content
        = yield
